@{
    ViewBag.Title = "Home Page";

}

<div class="jumbotron">
    <h1>Middle Valley Community Bank</h1>
    <p class="lead">MVC Bank offers the first-ever web-based ATM. Give it a try today!</p>
    <p><a href="http://asp.net" class="btn btn-primary btn-lg">Powered by ASP.Net</a></p>
</div>

<div class="row">
    @*<div class="col-md-4">
        <h2>Register </h2>
        <p>
            Come and open your account today!
        </p>
        <p>@Html.ActionLink("Register", "Register", "Account", null, new { @class = "btn btn-default" })</p>
    </div>
    <div class="col-md-4">
        <h2>Login</h2>
        <p>Access your acccount here</p>
        <p>@Html.ActionLink("Log in", "Login", "Account", null, new { @class = "btn btn-default" })</p>
    </div>*@
    
        <div class="col-md-5" id="formSection">
            <form method="get" id="limit">
                <h3>Number of transactions left: @(Convert.ToInt16(ViewBag.DailyLimit / 10)) /10</h3>
            </form>
        </div>
  

    
    <div class="row">
        <div class="col-md-12 margin-top-20">
            <a href="@Url.Action("Withdrawal", "Transaction", new { checkingAccountId = ViewBag.CheckingAccountId })" class="btn btn-primary btn-large btn-block"><span class="glyphicon glyphicon-circle-arrow-up"></span> Withdrawl (R10  Bank charges)</a>
            @*<a href="#" class="btn btn-primary btn-large btn-block"><span class="glyphicon glyphicon-flash"></span> Quick Cash R100</a>*@
            <a href="@Url.Action("Deposit", "Transaction", new  { checkingAccountId = ViewBag.CheckingAccountId })" class="btn btn-primary btn-large btn-block"><span class="glyphicon glyphicon-circle-arrow-down"></span> Deposit (R10  Bank charges)</a>
            <a href="@Url.Action("Details", "CheckingAccount")" class="btn btn-primary btn-large btn-block"><span class="glyphicon glyphicon-question-sign"></span> Balance Inquiry</a>
            <a href="@Url.Action("Statement", "CheckingAccount", new { checkingAccountId = ViewBag.CheckingAccountId })" class="btn btn-primary btn-large btn-block"><span class="glyphicon glyphicon-file"></span> View Statement</a>
            <a href="@Url.Action("Index", "SubscriptionModels", new { checkingAccountId = ViewBag.CheckingAccountId })" class="btn btn-primary btn-large btn-block"><span class="glyphicon glyphicon-list"></span> View All Subscriptions</a>
            <a href="@Url.Action("List", "CheckingAccount", new { checkingAccountId = ViewBag.CheckingAccountId })" class="btn btn-primary btn-large btn-block"><span class="glyphicon glyphicon-list"></span> View All Users</a>
            <a href="@Url.Action("Index", "Voucher", new { checkingAccountId = ViewBag.CheckingAccountId })" class="btn btn-primary btn-large btn-block"><span class="glyphicon glyphicon-list"></span> View All Vouchers</a>
        </div>
      
    </div>

</div>


@section Scripts{

@Scripts.Render("~/bundles/jqueryval")

<script>
            
   $('#formSection').html(response); 
                  
</script>

}

You are using @Request.Browser.Browser, and thats okay. 